# Copy to .env.local and fill in. Leave the Supabase values empty to run on built-in sample data.
# On Vercel, connecting the Supabase Marketplace integration sets these for you
# (it creates NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY, which are also accepted).

# Supabase project URL (Project Settings → API / Data API)
SUPABASE_URL=

# Supabase secret key: the new "sb_secret_..." key, or the legacy service_role key.
# SERVER-ONLY. Never prefix with NEXT_PUBLIC_ and never commit it.
SUPABASE_SECRET_KEY=

# Supabase publishable (or legacy anon) key. Used ONLY for the staff login session;
# RLS blocks it from reading any data. The Vercel integration sets SUPABASE_PUBLISHABLE_KEY.
SUPABASE_PUBLISHABLE_KEY=

# Who may sign in with Google (comma-separated, default figmints.com)
ALLOWED_EMAIL_DOMAINS=figmints.com

# Protects the scheduler endpoint (/api/cron/run-checks). Random string, 16+ characters.
# Generate one with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=

# Timezone used to display dates (optional, default America/New_York)
APP_TIMEZONE=America/New_York
